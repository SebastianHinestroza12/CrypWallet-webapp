import { SidebarWithHeader } from '../../components/SideBar';
import { Stack } from '@chakra-ui/react';

export const Home = () => {
  return (
    <Stack>
      <SidebarWithHeader />
    </Stack>
  );
};
